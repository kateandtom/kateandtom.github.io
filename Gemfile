# Optional — for local preview only. Publishing never requires this.
# GitHub Pages builds the site itself from the `main` branch.
source "https://rubygems.org"
gem "github-pages", group: :jekyll_plugins
gem "webrick"
